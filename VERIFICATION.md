# 验证记录 · 2026-09-18

## 自动检查

- Python 自动测试：73 项通过。
- 覆盖多活动拆分、按消息日期计算相对时间、区分活动年份与招录年度、日期星期冲突、无效时间和重复安排。
- 覆盖跨分片同秒分页、持久化消息进度、暂停开关、历史预览不自动发布、账号切换与活动去重。
- 覆盖主日历别名去重、网络中断后的相同请求编号、更新失败继续更新，以及 HTTP 跨站请求与 Host 校验。
- 覆盖按消息 MD5 与群定位原图、V1/V2/XOR 解码与损坏校验、文章标题/正文提取、实际起止时间、低置信度进入核对、自动重试与识别过程中的忽略操作。
- 覆盖图片和文章进入正常自动发布流程、重复处理不重复创建、部分文章超出读取上限保留候选而非无限重试。
- 覆盖个人信息保存接口、性别与年级条件匹配、多个候选人员范围的排除与自动发布，以及单点时间按下一场或 30 分钟推断。
- JavaScript 语法检查与 Python 编译检查通过。

## 本机集成检查

- 使用已配置的微信数据库进行了只读验证：多活动通知正确拆分，年份、各环节时间与地点和原文对应。
- 浏览器验证了同步概览、群设置、日历选择、历史预览、手动粘贴通知与识别结果。
- 浏览器验证了“连接与设置 → 我的个人信息”区域：个人资料字段能够从本机设置加载，页面说明明确匹配自动同步、不匹配跳过、资料不足才待确认。
- 官方 `lark-cli` 对真实主日历的创建请求 dry-run 返回成功。此前真实创建的日程已导入本地索引，导入过程没有重复创建。
- 用户级 systemd 服务和应用菜单入口已验证，服务仅监听本机地址；已启用的群按配置周期检查新消息。
- 验证了已有飞书登录的复用，以及补充日历列表读取权限后返回真实可选日历。
- 界面将已有连接与可选的补充权限分开显示，不把读取列表权限作为使用已知主日历的前提。

## 图片与文章的实际链路

- 通过消息元数据、微信附件索引及完整 MD5 匹配读取了一条真实 V2 海报，并在本机完成 OCR。活动名称、日期、起止时间和地点与原图人工核对一致。
- 通过 App 的重新识别接口进入既有自动发布流程，飞书创建接口返回日程编号与 App Link；本机同步记录显示成功。未为验证额外创建测试日程。
- 读取了一条真实招聘分享链接，使用页面公开接口实际公布的结束时间，未猜测时长。识别时活动已结束，结果按既有规则保留为待确认。
- 用户明确忽略的另一条文字消息仍为 ignored；模型辅助保持关闭。
- 使用虚构个人资料和通知条件验证：匹配“35级全体男生”的活动自动发布，明确不匹配的候选被排除，未将任何真实个人资料写入仓库。
- 真实运行服务已备份数据库、完成增量字段迁移并重启；浏览器验证了来源标记、已同步日程详情、原图预览和 OCR 文字。
- 当前登录缺少日程详情读取权限；额外的 `calendar +get` 校验未执行成功。实际写入成功依据为创建接口返回编号与链接，没有要求用户为此重复授权。

## 验证边界

- 测试中的通知、账号和群标识使用示例数据，仓库不包含真实聊天记录或运行数据库。
- 公开文档中的人员条件示例均为虚构数据；真实用户性别、年级、群名、消息编号和飞书链接只存在于本机运行数据，不进入仓库。
- 可选模型调用分支未向真实模型服务发送群消息；当前集成验证覆盖本地规则识别。
- 已验证本机 Linux 微信 V2 海报与云就业分享链接；公众号 HTML/懒加载图片、URL 限制使用合成内容测试，不保证所有公众号页面和微信版本均可读取。语音、视频和文件正文暂不自动解析。

本文件记录测试范围；当前运行状态请以 App 的连接状态、同步开关与最近检查时间为准。
